# SY205 GLB Inspection

## Source identity

- Source: `E:/projects/blender/Excavator/SY205/export/godot/SY205_excavator_godot.glb`
- Size: `4,904,884` bytes
- SHA-256: `cf95534b31bcc156980afefef0a9f273e5c6f727547b3db1e9062ca5619b495a`
- glTF: 2.0, generated by Khronos glTF Blender I/O v5.2.39
- GLB chunks: JSON 16,008 bytes; BIN 4,888,848 bytes

## Measured contents

- One scene, 20 nodes, 11 meshes, 11 primitives, 9 materials, 2 embedded PNG images, 1 sampler.
- Images are `lvdai_T` (512×512) and `Excavator_T` (2048×2048); materials reference them through the embedded buffer, so no external texture sidecar is required.
- No `extensionsUsed` or `extensionsRequired` entries.
- No skins, animations, morph targets, or explicit collision meshes/resources.
- Godot 4.7.1 initially defaulted to extracting the embedded images beside the GLB. The reviewed import setting uses `gltf/embedded_image_handling=2` (Embed as Basis Universal) so the runtime scene remains self-contained and generated PNG sidecars are not repository inputs.
- Position accessors contain normals and UVs. The mesh index counts sum to 10,955 triangles.
- Raw transformed geometry is approximately 3.16 m wide × 6.59 m high × 7.15 m deep before any Godot-side presentation calibration. Treat this as an inspection bound, not an authority scale; verify imported Godot bounds.
- Godot 4.7.1 headless import measured the same bounds in the imported scene: min `(-1.579262, 0.010844, -2.923488)`, max `(1.579262, 6.600203, 4.231390)`, size `(3.158524, 6.589359, 7.154878)`.

## Mechanical hierarchy

```text
CTRL_EXCAVATOR_ROOT
├── crawler_tracks
├── PIVOT_SLEW
│   ├── upper_body
│   └── PIVOT_BOOM_BASE
│       ├── boom
│       └── PIVOT_ARM_JOINT
│           ├── excavator_arm
│           ├── CTRL_LINKAGE_SIDE_LINKS
│           │   ├── bucket_linkage_secondary_a
│           │   └── bucket_linkage_secondary_b
│           ├── PIVOT_BUCKET_JOINT
│           │   ├── bucket
│           │   └── PIVOT_LINKAGE_C_BUCKET
│           └── PIVOT_LINKAGE_B_ARM
│               ├── bucket_linkage_primary
│               └── PIVOT_LINKAGE_A_COMMON
│                   └── bucket_linkage_connector
└── undercarriage_chassis_left
    undercarriage_chassis_right
```

The source extras label the five mechanical pivots as: root/undercarriage (`rotation_axis=Z`), upper slew (`Z`), boom base (`X`), arm hinge (`X`), and bucket hinge (`X`). This agrees with the Python URDF's swing/boom/arm/bucket joint-axis split.

## Adapter recommendation

Use the imported GLB as one `PackedScene` instance and preserve its hierarchy. Map protocol frame names to pivot paths:

| Python frame | GLB node path | Driven visual content |
|---|---|---|
| `base_link` | `CTRL_EXCAVATOR_ROOT` | tracks and both chassis meshes |
| `upper_structure_link` | `CTRL_EXCAVATOR_ROOT/PIVOT_SLEW` | upper body plus downstream work group |
| `boom_link` | `CTRL_EXCAVATOR_ROOT/PIVOT_SLEW/PIVOT_BOOM_BASE` | boom plus downstream arm group |
| `arm_link` | `CTRL_EXCAVATOR_ROOT/PIVOT_SLEW/PIVOT_BOOM_BASE/PIVOT_ARM_JOINT` | arm and linkage group |
| `bucket_link` | `CTRL_EXCAVATOR_ROOT/PIVOT_SLEW/PIVOT_BOOM_BASE/PIVOT_ARM_JOINT/PIVOT_BUCKET_JOINT` | bucket and bucket-side reference pivot |

The linkage meshes stay in the supplied hierarchy and are visual auxiliaries in the first slice. They are not added to the four-joint protocol.

At runtime, the adapter should apply Python `frame_transforms` to the mapped pivot nodes only after capturing imported rest transforms. The calibration must be measured in Godot and checked against `backend/tests/fixtures/frame-parity/baseline.json`; raw glTF row/column conventions and the Blender-to-Godot axis conversion must not be guessed.

The headless import captured these rest-pose global transforms (row-array form used by the Godot-local manifest): base `(0, 0.449999988, 0)`, slew `(0, 0.910000026, 0)`, boom `(-0.119000003, 1.623000026, -0.075000003)`, arm `(-0.053000003, 5.918000221, 3.839999914)`, and bucket `(-0.061000004, 2.891999960, 3.210000038)`, all with identity basis. These are imported-asset rest measurements, not yet Python frame parity results.

## Risks and deferred checks

1. The GLB has no animation or collision resources. Motion must be driven by the adapter; local colliders remain optional and separate.
2. The asset is a combined scene, while the legacy backend manifest describes five separate assets. Keep the legacy manifest untouched and use a Godot-local mapping manifest.
3. The supplied root and mesh nodes contain Blender/glTF rotation quaternions. Imported Godot global transforms must be inspected before setting a global scale/orientation calibration.
4. The first implementation should prove a zero-pose and asymmetric-pose frame-parity fixture before adding PBR polish or soil effects.
