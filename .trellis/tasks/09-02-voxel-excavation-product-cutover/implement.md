# Implement - product cutover

- [ ] Load and verify archived evidence from the first three children; do not
  reopen their algorithm decisions without a failed integration contract.
- [ ] Integrate product scene, lifecycle, Jolt traversal/support gating, both
  models, quality modes, Bucket Pass, HUD/advanced diagnostics, and effects.
- [ ] Add focused end-to-end tests for entry/traversal/parking, active editing,
  deposits, collision lag, reset/model switch/pause/teardown, seam ownership,
  and stale generation rejection.
- [ ] Audit Python/Gateway/CAN diffs and run only focused compatibility checks if
  those layers are untouched.
- [ ] Stabilize with focused tests, then run the full Godot matrix once and one
  representative performance gate; retain concise artifacts.
- [ ] Provide the exact human both-model Forward+ acceptance checklist and wait
  for explicit acceptance.
- [ ] Record the last pre-deletion rollback commit.
- [ ] Make voxel authority the only product path; delete obsolete heightfield
  excavation/active soil/loose flux/parcel/collider/toggle code and invalidated
  tests. Re-run only directly affected focused gates, then the justified final
  matrix if shared deletion changes it.
- [ ] Update specs, architecture, notices, validation docs, and provenance.
- [ ] Do not build distributions unless the user explicitly requests them.
- [ ] Commit, push, archive the child and parent only after final user approval.

The cutover is blocked by any unaccepted human milestone, double collider,
unbounded queue, cumulative mass drift, stale generation resurrection, or
non-soil regression.

